<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="light" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>

        <!-- Color -->
        <item name="themesAccueilBackground">#EEEEEE</item>
        <item name="themesAccueilTitle">@color/black</item>
        <item name="themesBackgroundColor">@color/white</item>
        <item name="themesTextColor">@color/black</item>
        <item name="themesCalcButton">@color/firefly</item>

    </style>

    <style name="daynight" parent="@style/Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryNight</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkNight</item>
        <item name="colorAccent">@color/colorAccentNight</item>

        <!-- Color -->
        <item name="themesAccueilBackground">#111111</item>
        <item name="themesAccueilTitle">@color/white</item>
        <item name="themesBackgroundColor">@color/black</item>
        <item name="themesTextColor">@color/white</item>
        <item name="themesCalcButton">#ecccc8</item>

    </style>
</resources>